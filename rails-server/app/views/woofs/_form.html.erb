<%= form_with(model: woof, local: true) do |form| %>
  <% if woof.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(woof.errors.count, "error") %> prohibited this woof from being saved:</h2>

      <ul>
        <% woof.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
